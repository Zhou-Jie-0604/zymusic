#!/usr/bin/env python3
"""Generate ZYmusic academic report, target ~14000 chars (Word shows ~10000)."""
from docx import Document
from docx.shared import Pt, Cm
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.oxml.ns import qn

doc = Document()

for sec in doc.sections:
    sec.page_width = Cm(21.0); sec.page_height = Cm(29.7)
    sec.top_margin = Cm(2.54); sec.bottom_margin = Cm(2.54)
    sec.left_margin = Cm(3.18); sec.right_margin = Cm(3.18)

style = doc.styles['Normal']
style.font.name = '宋体'; style.font.size = Pt(10.5)
style.element.rPr.rFonts.set(qn('w:eastAsia'), '宋体')
style.paragraph_format.line_spacing = 1.5
style.paragraph_format.space_before = Pt(0)
style.paragraph_format.space_after = Pt(0)

def sf(run, name='宋体', size=Pt(10.5), bold=False):
    run.font.name = name; run.element.rPr.rFonts.set(qn('w:eastAsia'), name)
    run.font.size = size; run.bold = bold

def sp(p):
    p.paragraph_format.line_spacing = 1.5
    p.paragraph_format.space_before = Pt(0)
    p.paragraph_format.space_after = Pt(0)

def blank(n=1):
    for _ in range(n):
        p = doc.add_paragraph(); sp(p)

def pb():
    doc.add_page_break()

def cover_line(text, size=Pt(16), bold=False):
    p = doc.add_paragraph(); p.alignment = WD_ALIGN_PARAGRAPH.CENTER; sp(p)
    r = p.add_run(text); sf(r, '宋体', size, bold)

# 摘要标题: 四号 宋体 加粗 居中
def abstract_title(text):
    p = doc.add_paragraph(); p.alignment = WD_ALIGN_PARAGRAPH.CENTER; sp(p)
    r = p.add_run(text); sf(r, '宋体', Pt(14), True)

# 摘要内容: 小四号 宋体
def abstract_body(text):
    p = doc.add_paragraph(); p.paragraph_format.first_line_indent = Pt(24)
    p.alignment = WD_ALIGN_PARAGRAPH.JUSTIFY; sp(p)
    r = p.add_run(text); sf(r, '宋体', Pt(12))

# 一级标题: 四号 宋体 居中加粗
def h1(text):
    p = doc.add_paragraph(); p.alignment = WD_ALIGN_PARAGRAPH.CENTER; sp(p)
    r = p.add_run(text); sf(r, '宋体', Pt(14), True)

# 二级标题: 小四号 宋体 靠左加粗 (一、格式)
def h2(text):
    p = doc.add_paragraph(); p.alignment = WD_ALIGN_PARAGRAPH.LEFT; sp(p)
    r = p.add_run(text); sf(r, '宋体', Pt(12), True)

# 三级标题: 小四号 宋体 靠左加粗 (1.格式)
def h3(text):
    p = doc.add_paragraph(); p.alignment = WD_ALIGN_PARAGRAPH.LEFT; sp(p)
    r = p.add_run(text); sf(r, '宋体', Pt(12), True)

# 正文: 五号 宋体 首行缩进2字符
def body(text):
    p = doc.add_paragraph(); p.paragraph_format.first_line_indent = Pt(21)
    p.alignment = WD_ALIGN_PARAGRAPH.JUSTIFY; sp(p)
    r = p.add_run(text); sf(r, '宋体', Pt(10.5))

# 参考文献: 小四号 宋体
def ref_line(text):
    p = doc.add_paragraph(); p.paragraph_format.first_line_indent = Pt(0)
    p.alignment = WD_ALIGN_PARAGRAPH.JUSTIFY; sp(p)
    r = p.add_run(text); sf(r, '宋体', Pt(12))

def img(text):
    p = doc.add_paragraph(); p.alignment = WD_ALIGN_PARAGRAPH.CENTER; sp(p)
    r = p.add_run(text); sf(r, '宋体', Pt(10.5))

# ==================== COVER PAGE ====================
blank(6)
cover_line('Java开发技术大作业报告', Pt(26), True)
blank(1); cover_line('ZY音乐系统', Pt(18)); blank(2)
for line in ['专业：大数据管理与应用', '学号：092490101、092490102', '姓名：周婕、李杨']:
    cover_line(line, Pt(16))
blank(3); cover_line('2024年5月', Pt(16))

# ==================== 摘要 ====================
pb()
abstract_title('摘要')

abstract_body('传统音乐平台普遍存在资源自主管理不足、社交互动深度欠缺和个性化表达受限等问题。本文设计并实现了一款基于浏览器-服务器架构的音乐社交系统——"ZY音乐"，重点从服务端数据处理算法与客户端交互算法两个维度展开系统化阐述。')

abstract_body('服务端基于分层架构模式构建了五层数据处理模型，采用邻接表模型实现评论数据的嵌套树形存储与查询，通过通配符模式匹配实现多维模糊检索，利用检查-切换原子操作保证交互状态一致性，基于HTTP范围分段请求实现音频流式传输，并通过幂等建表与元数据检测结合的增量迁移算法保障数据库迭代中的数据完整性。客户端基于立即执行函数表达式单例模式配合持久化存储解决跨页面播放状态共享问题，采用有限状态机模型定义多模式播放控制，通过Canvas二维图形接口的圆形裁剪与仿射变换构建纯客户端图像编辑管线，基于服务线程缓存优先策略实现离线访问，利用递归树遍历算法完成评论嵌套结构的动态渲染。系统以嵌入式Web容器和本地关系型数据库为基础设施，通过桌面端Web引擎封装与渐进式Web应用配置实现了多终端统一访问。')

abstract_body('关键词：分层架构；邻接表模型；范围分段传输；有限状态机；Canvas图形裁剪；缓存优先策略')

# ==================== 目录 ====================
pb()
h1('目  录')
body('（请在Word中右键此处，选择"更新域"以自动生成目录）')

# ==================== 绪论 ====================
pb()
h1('绪论')

h2('一、问题背景')
body('音乐作为基础性的精神文化产品，在当代日常生活中占据日益重要的地位。随着数字音频编码技术的成熟和移动通信网络的持续普及，用户对音乐平台的期望已从被动的在线收听逐步延伸至音频资源自主管理、个性化内容组织以及以音乐兴趣为纽带的社交互动等多个维度。然而，当前主流商业音乐平台多采用中心化云端服务架构，用户仅能在平台预先设定的功能边界内进行歌曲搜索、收藏和评论等有限操作，既无法将本地拥有的音频文件上传至平台进行统一管理与分享，也难以围绕音乐资源构建个性化的社交关系网络，在资源归属权、内容管理自主性和社交互动深度等方面均存在明显局限。')

body('从算法设计与系统架构的视角加以审视，构建一个功能完整的音乐社交平台需要解决若干核心技术问题。其一，如何设计合理的分层数据处理架构，使得系统在保持模块职责分离的前提下实现高效的请求分发与数据流转——这涉及软件架构理论中的关注点分离原则和依赖倒置原则。其二，如何选用合适的树形数据存储模型来承载评论等具有天然层级关系的数据，在插入删除的局部性和查询遍历的效率之间取得适用于本场景的平衡。其三，如何在客户端以严谨而简洁的算法模型实现播放状态管理、图像交互处理和离线缓存策略等功能。上述问题的解决涉及数据结构理论、状态机模型、图形学基础以及网络协议等多个计算机科学分支的知识综合运用，将它们统一于一个可实际运行的系统之中并加以理论化的系统阐述，具有明确的实践意义与教学价值。')

h2('二、当前研究现状')
body('Web应用架构领域，分层架构模式自二十世纪九十年代由Buschmann和Fowler等人系统总结以来已成为企业级应用设计的核心范式，该模式将系统按数据处理职责垂直划分为多个层次，各层之间遵循单向依赖原则以实现关注点的有效分离。与之并行的模型-视图-控制器范式由Krasner和Pope在Smalltalk-80环境中首次系统阐述，其核心思想是将应用程序划分为模型、视图和控制器三个相互解耦的组件，在Java平台生态中两者的融合催生了前端控制器、数据访问对象和传输对象等成熟设计模式，为Web应用的工程化实现提供了系统的模式语言。')

body('树形数据存储方面，关系型数据库中层次结构的表达是一个经典且持续被研究的问题。Celko在其著作Trees and Hierarchies in SQL for Smarties中系统梳理了邻接表、路径枚举、嵌套集合和闭包表四种主要方案——邻接表通过在每个节点上记录父节点标识符维护层级关系，实现简洁但递归查询效率受限；路径枚举以路径字符串编码存储，单次查询即可获取子树但路径维护开销大；嵌套集合通过左右值编码将树映射为区间包含关系，查询性能优越但插入删除需批量更新；闭包表以独立路径表存储所有祖先-后代对，查询最灵活但空间开销大。在实际项目中，存储模型的选择取决于读写负载特征和可维护性需求的综合权衡。')

body('流媒体传输领域，Fielding和Reschke于2014年发布的RFC 7233正式标准化了HTTP范围分段请求机制，详细定义了Range请求头语法、206 Partial Content响应状态码语义、Content-Range响应头格式以及多段范围的请求与响应规则，使客户端无需完整下载大型媒体文件即可从任意字节偏移位置开始接收数据流。客户端算法领域，W3C制定的HTML5标准引入了Canvas二维图形绘制接口和Audio音频播放接口，为浏览器端的像素级图像处理和音频解码控制提供了无需插件的标准化编程接口。Service Worker服务线程技术的出现则为渐进式Web应用提供了独立于页面生命周期的后台请求拦截和可编程缓存管理能力。')

body('将上述分散于各技术分支的算法模型综合集成到一个功能完整的音乐社交系统中，并从服务端算法与客户端算法两个维度进行系统化的理论阐述，在已有课程设计报告和教学文献中仍不多见，这正是本文工作的出发点与核心价值所在。')

h2('三、研究创新点')
body('与传统项目报告侧重于功能罗列不同，本文以算法为叙述主线，将系统开发重构为服务端数据处理和客户端交互两个理论板块。数据组织上，采用邻接表模型通过单表配合自引用字段统一承载音乐评论和帖子评论两类场景，并设计了客户端两阶段索引映射与递归遍历的扁平-树形转换算法。流媒体传输上，完整实现了基于HTTP Range协议的字节级流控算法，包含请求解析、边界校验、随机访问定位和分段缓冲写入四个环节，同时设计了多级回退的音频格式适配转换链。播放器架构上，基于单例模式与持久化存储构建了跨页面状态共享模型，通过有限状态机对三种播放模式进行了形式化定义。数据库迭代管理上，设计了基于模式元数据列检测的幂等迁移算法，实现了零数据损失的结构版本化演进。')

h2('四、技术路线')
body('系统以Java语言为核心，选用嵌入式Web容器作为运行时基础设施——该容器在程序启动时自动初始化HTTP服务并加载Web应用上下文，无需外部中间件安装配置。数据持久化采用JDBC原生接口操作SQLite数据库，表结构管理在首次启动时自动完成。页面渲染使用JSP服务端动态模板在服务器端完成HTML生成，通过Gson库实现JSON序列化以支持客户端异步数据交互。')

body('客户端基于HTML5标准接口和原生JavaScript开发，未引入额外框架。音频播放基于Audio接口实现控制逻辑，头像裁剪基于Canvas接口完成图像处理，异步交互基于Fetch接口实现，离线缓存通过Service Worker配置。桌面端通过JavaFX WebView组件将页面封装为原生应用，移动端通过PWA清单配置和Service Worker脚本提供可安装的离线体验。三方共享同一套核心代码与页面模板。')

img('（这里需要插入一张系统整体技术架构图）')

# ==================== 服务端算法 ====================
pb()
h1('服务端数据处理模型与核心算法设计')

h2('一、系统分层数据处理模型构建')
body('服务端采用五层分层数据处理架构，自上而下依次为页面表示层、请求分发控制层、业务规则逻辑层、数据持久访问层和物理数据存储层。各层之间遵循严格的单向依赖原则——上层仅调用直接下层提供的接口，不允许跨层访问或反向依赖。页面表示层在服务端完成HTML的动态生成，模板引擎通过读取控制层传递的数据对象及会话中的用户信息来生成页面内容。控制层作为所有请求的统一入口，负责HTTP方法的区分、参数提取与校验以及响应的转发或重定向，自身不包含业务规则，契合前端控制器模式的核心思想。逻辑层集中承载身份认证、资源管理、歌单组织、评论交互和内容检索等核心处理单元，将业务规则集中封装以提升可复用性和可测试性。持久访问层统一通过参数化预编译语句执行查询并采用自动资源管理语法确保连接对象的可靠释放。存储层由本地数据库文件和按音频、封面、头像、背景、歌单封面等类别分目录组织的上传文件目录组成。')

img('（这里需要插入一张系统分层架构示意图）')

h2('二、核心业务处理模型设计')
body('用户身份认证模型基于Servlet会话抽象机制实现跨请求状态保持。认证服务采用多路查找策略——先以用户名在用户信息表中精确匹配，未果时转为以手机号二次检索，使用户可通过任一凭证完成认证。会话标识符在认证成功后写入客户端，后续请求据此恢复会话上下文，从而在无状态HTTP协议之上构建有状态的会话管理。对需身份验证的功能访问，控制层在处理前检查会话中是否存在有效用户对象，未认证请求被重定向至登录页面。')

body('音频资源上传处理模型通过声明式多部件配置设定文件尺寸上限，客户端以多部件表单编码提交音频数据流、封面图像数据流及文本元数据。服务端按预定顺序提取数据部件后实施多层校验：数据量不超过百兆上限、格式扩展名在允许的编码格式列表内、封面图像的MIME类型需为合法位图格式。校验通过后采用UUID算法生成全局唯一存储文件名以防止命名冲突，数据库仅保存相对路径以保障系统迁移时的可移植性。')

body('音频格式适配模型设计了"转换优先、原始保留"的多级回退策略链。对于FLAC无损压缩格式，系统优先调用FFmpeg将其转换为320kbps的MP3格式以获取广泛浏览器兼容性；若工具不可用或转换失败，则通过自实现解码器转为WAV波形格式并缓存；若所有转换路径均失败，以原始FLAC格式直接提供播放。这种逐级回退的容错设计保障了系统在各种运行环境下均能完成音频处理任务。')

img('（这里需要插入一张音乐上传与格式转换处理流程图）')

h2('三、关键算法设计与分析')

h3('1. 基于邻接表模型的评论嵌套树存储与查询算法')
body('评论系统的核心需求是同时支撑音频评论和帖子评论两类场景并允许任意深度嵌套回复。在关系型数据库中存储树形层次数据，主要有邻接表、路径枚举、嵌套集合和闭包表四种方案。本系统选用邻接表模型——在每个树节点上存储其直接父节点标识符，以此隐式表达父子关系。数据库使用统一的评论关系表承载两种语义：通过归属对象标识符区分评论类型，通过父节点自引用字段记录回复目标，为零表示顶层评论，非零表示对已有评论的直接回复。这种单表设计避免了为不同类型评论分别建表带来的查询逻辑重复和代码冗余。')

body('从关系代数的角度对该数据模型加以形式化描述：设评论关系R的全体元组构成有限集合C，每条评论元组c ∈ C具有属性集合{id, content, userId, username, musicId, postId, parentId, parentUsername, timestamp}。对于给定音乐资源m的评论树查询，首先通过一元选择操作获取所有位于树根位置的顶层评论节点：σ(musicId=m.id ∧ parentId=0)(C)；随后对于顶层集合中的每一个评论节点p，再次通过选择操作获取其直接子回复节点集合：σ(parentId=p.id)(C)。上述选择过程可按深度优先或广度优先策略递归应用于每一个非叶子节点，直至某层级所有节点均无子节点时递归自然终止。')

body('工程实现中，服务端以单次结构化查询返回目标内容下的全部评论列表（按创建时间降序排列），以JSON数组形式传输至客户端。客户端树形重建采用两阶段算法：第一阶段为数据结构化预处理——单次遍历扁平列表，建立以评论标识符为键的哈希映射表以提供O(1)常数时间的随机访问能力，同时将每条评论按父节点标识符分流收集——为零者归入根节点数组，非零者追加至子评论映射表中。第二阶段为递归文档对象生成——对根节点数组中的每个顶层评论调用递归渲染函数，传入评论对象和当前嵌套深度；函数根据深度参数计算左侧CSS缩进偏移量（深度×固定缩进像素值），生成包含作者头像、显示名称、评论文本和相对时间戳的HTML片段以及嵌套子容器元素；随后以当前评论标识符查询子评论映射表，若非空则对每条子评论以深度加一递归调用自身并将结果追加至当前容器，若为空则递归终止。邻接表模型在评论增删频繁而深层递归查询较少的场景中具有维护简洁的明显优势——插入新评论仅需一条记录，删除同样只需移除单条记录而无需更新其他任何行，在本系统数十条量级的评论规模下性能完全可接受。')

h3('2. 基于通配符模式匹配的多维模糊检索算法')
body('系统提供覆盖歌曲名称、艺术家名称、流派类别和上传者名称四个维度的全局检索功能。检索算法基于SQL LIKE通配符模式匹配机制——将用户输入的关键词嵌入两个百分号通配符之间形成匹配模式，百分号代表任意长度的任意字符序列，从而实现对目标字段任意位置出现关键词的全部匹配。从字符串匹配理论看，这是正则表达式有限自动机匹配的一种简化工程实现，将搜索意图转化为对关系表中指定列的子串包含性布尔判定。')

body('算法复杂度分析：设音乐记录数为n，匹配字段数为k，单次搜索需执行kn次子串匹配。在未建立全文索引时，数据库引擎退化为全表扫描，时间复杂度为O(knm)，m为平均字段长度。在本系统面向的单机场景下（音乐量级为数百至数千首），搜索延迟处于毫秒量级。若未来数据规模增长，可引入基于倒排索引的全文检索引擎或向量空间模型以在亚线性时间内完成相关性排序检索。')

h3('3. 基于检查-切换原子操作模式的交互状态翻转算法')
body('音乐点赞功能需在用户每次触发时完成已赞与未赞状态的确定翻转，同时更新点赞计数。该操作本质上是状态翻转函数f满足f(已赞)=未赞且f(未赞)=已赞。数据库层面的完整执行涉及检查记录存在性、根据结果决定删除或插入、更新计数字段三个步骤。在并发场景下，若不加以协调，两个并发的翻转可能先后读到相同的"未赞"状态并各自插入，导致计数与实际记录数不一致。本系统将检查、写入和计数更新置于同一数据库连接的生命周期内顺序执行，利用SQLite在WAL模式下对单连接的串行化语义保证操作的整体原子性，无需引入分布式锁或应用层互斥同步机制。服务端完成后将更新后的点赞数量和用户状态以JSON返回，前端据此即时更新界面。')

h3('4. 基于HTTP范围分段请求的字节级流式传输算法')
body('音频在线播放要求客户端能以最小延迟开始音频输出并在进度拖拽时从目标时间点精确恢复解码，其技术基础是IETF RFC 7233标准化的范围分段请求机制。本系统在文件服务模块中完整实现了该协议层算法。')

body('服务端处理流程分为四个步骤。解析阶段：提取Range请求头中"bytes="前缀后的数值字符串，按连字符"-"分割为起始位置和结束位置的字符串表示，分别解析为长整型数值。需处理两种边界语法——连字符在末尾（"start-"形式，仅指定起始位置）时结束位置取文件末尾偏移量；连字符在起始（"-suffix"形式）时起始位置取文件总长度减去suffix值。校验阶段：若起始位置大于等于文件总长度或起始位置严格大于结束位置，返回416 Range Not Satisfiable状态码并声明Content-Range: bytes */总长度。定位阶段：使用RandomAccessFile打开目标文件，调用seek方法将底层文件读取指针精确移动至请求的起始字节偏移量处，跳过前置数据而不读取。传输阶段：计算传输量contentLength = end - start + 1，以8KB固定大小缓冲区在while循环中读取并写入HTTP响应输出流，每轮迭代递减remaining直至为零或文件读取完毕。')

body('该算法的核心数学映射关系为：对文件长度L，合法请求区间[start, end]满足0 ≤ start ≤ end < L，单次响应传输量为(end - start + 1)字节。客户端根据当前播放时间t、总时长T和文件总字节数L，通过比例公式start = ⌊(t / T) × L⌋计算出目标时间点对应的近似字节偏移量并据此构造Range请求头。对于固定码率编码的MP3等音频格式，时间-字节的线性比例映射具有物理意义上的一一对应关系，计算精度足以支持秒级乃至亚秒级的逐帧播放定位；对于可变码率音频，定位精度依赖音频内部的解码时间戳元数据，在浏览器原生Audio接口辅助下同样可实现较为精确的时间点恢复。')

img('（这里需要插入一张HTTP范围请求与响应的时序交互图）')

h3('5. 基于元数据检测的幂等数据库演进算法')
body('系统在持续迭代中需向已有数据表新增字段，传统做法要求手动删除旧数据库并重建，导致既有数据永久丢失。本系统设计了一种基于模式元数据检测的增量列迁移算法。算法由两部分互补构成：幂等建表语法CREATE TABLE IF NOT EXISTS在每次启动时执行——表已存在时不操作，不存在时按预定义结构创建，确保初始版本正确建立且重复启动无副作用；列级增量检测机制通过DatabaseMetaData.getColumns接口检查指定列是否已存在于数据库模式中，若缺失则执行ALTER TABLE ADD COLUMN追加新列并设默认值，若已存在则跳过。这一"检测-补充"过程的幂等性保证无论执行多少次，最终状态始终一致。在系统开发迭代中，累计十五个业务字段通过此机制被逐步添加，涵盖用户信息、音乐资源、评论关系和歌单关联等模块，实现了零数据损失的版本化结构演进。数据库初始化同步设置了WAL日志模式、5000ms忙等待超时和NORMAL同步级别等运行时参数，为潜在并发访问提供基础容错能力。')

# ==================== 客户端算法 ====================
pb()
h1('客户端交互模型与核心算法设计')

h2('一、客户端状态管理模型构建')
body('用户界面运行环境为现代浏览器，交互功能基于W3C HTML5标准接口和原生JavaScript实现，未依赖第三方框架。客户端架构围绕以下原则展开：全局共享音频播放状态通过IIFE单例模块集中管理以避免多页面实例冲突；播放器状态通过localStorage实现跨页面保存与恢复；界面渲染与数据获取通过事件驱动机制解耦；异步网络交互通过Fetch API实现无刷新更新。')

body('客户端脚本按功能职责分解为五个独立模块：动画模块负责背景落叶粒子系统；播放器中枢模块以IIFE模式管理全局唯一Audio实例，作为播放功能的数据与状态中心；播放页控制模块实现播放器界面交互逻辑；上传辅助模块处理封面图像本地预览；个人主页定制模块负责头像裁剪交互管线。各模块通过window全局对象上注册的命名空间方法通信——中枢模块对外暴露播放控制、音频实例获取和播放列表管理等API，其他模块通过调用这些接口操作共享的Audio元素，避免了多页面中重复创建Audio对象导致的资源浪费和状态不一致。')

img('（这里需要插入一张前端模块结构与数据流图）')

h2('二、核心交互功能设计')
body('全局播放栏模块以IIFE形式封装，页面加载后自动初始化。模块检测全局唯一性标志位确保同一页面仅执行一次初始化，内部创建唯一Audio元素并维护当前歌曲、播放状态、播放列表和时长四个状态变量，音量初始值从localStorage读取用户历史设置。')

body('播放状态跨页面持久化通过localStorage实现。状态变更时模块将当前歌曲元数据、播放列表、状态标志、音量和当前时间序列化为JSON写入存储；新页面加载时从存储读取状态快照反序列化并恢复，页面卸载前触发最后一次保存。这套"即时保存-完整恢复-最终持久"的三段式策略使用户在不同页面间导航时获得接近单页应用的连续听觉体验。对音频播放位置的恢复采用延迟定位策略——在Audio元素的loadedmetadata事件回调中执行时间位置的具体跳转，确保时长数据可用后再进行定位计算。')

body('事件通知感知模块通过定时轮询实现新消息的准实时感知。页面加载后获取通知列表并记录首条标识符作为基准，每十秒轮询将最新标识符与基准比较——数值增大则说明有新事件，据此更新未读计数并更新基准值。这种基于单调递增标识符的增量检测策略将每次轮询复杂度控制在O(1)。')

img('（这里需要插入一张播放器状态跨页面管理流程图）')

h2('三、关键算法设计与分析')

h3('1. 基于单例模式与持久化存储的全局播放器共享算法')
body('传统多页面Web应用中每次导航都会销毁前一页面的脚本环境并重建Audio实例，导致播放中断和进度丢失。本系统通过单例模式配合localStorage持久化存储解决了这一工程难题。播放器中枢模块的代码体被包裹在定义后立即调用的匿名函数内部，其执行在全局对象上注册公共方法接口，而内部定义的所有状态变量被封装在闭包的作用域链中，外部代码只能通过暴露的公共方法进行受控的间接操作，从程序设计语言理论角度实现了信息的隐藏与访问控制。跨页面状态传递以localStorage为持久化介质，保存逻辑在每次状态变更后立即触发，恢复逻辑在页面加载时执行。')

h3('2. 基于有限状态机模型的多模式播放控制算法')
body('播放器提供循环、顺序和随机三种播放模式。模式切换由一个环形状态转换函数控制——每次触发时当前模式按固定序列转移至下一状态，即mode_next = (mode_current + 1) mod 3。模式状态值通过localStorage保存以跨会话保留用户偏好。模式影响的核心行为是歌曲结束后下一首的选择策略：循环模式直接返回当前索引使同一歌曲无限重复；顺序模式返回索引加一，超出列表范围时返回终止标识，播放器停止并等待用户手动操作；随机模式采用拒绝采样算法——生成[0, n-1]均匀随机整数ri，若ri等于当前索引则重试直至不等。每次采样拒绝概率为1/n，期望采样次数为n/(n-1)，对于常见列表规模摊销时间复杂度为O(1)。')

h3('3. 基于Canvas圆形裁剪与仿射变换的头像编辑算法')
body('头像定制功能允许用户上传任意尺寸位图并通过拖拽缩放交互精确调整圆形裁剪区域内的显示内容，全部图像处理在客户端完成。算法的数学核心是构建从源图像坐标系到裁剪画布坐标系的二维仿射变换映射。设源图像尺寸为iw×ih，裁剪画布边长size，基础缩放比例baseScale = max(size/iw, size/ih)取宽高比中的较大值，确保缩放后图像在保持宽高比前提下一个维度完全填满裁剪区。用户通过滑块可额外施加zoomScale倍缩放，通过拖拽产生累积偏移量dx和dy，最终合成比例scale = baseScale × zoomScale。')

body('图像在画布上的绘制锚点坐标由以下仿射变换公式唯一确定：anchorX = (size - iw×scale)/2 + dx，anchorY = (size - ih×scale)/2 + dy。公式中前一项(size - iw×scale)/2执行将缩放后图像在画布上精确居中放置的自动定位计算，后一项dx则施加用户在水平方向上通过拖拽操作累积产生的平移偏移量；垂直方向公式结构与水平方向完全对称。')

body('Canvas渲染流水线按严格顺序经历五个阶段。第一阶段：调用save方法将当前完整绘图状态快照压入渲染引擎内部状态栈。第二阶段：调用beginPath开启新几何路径，以arc方法围绕画布中心(size/2, size/2)创建一个精确圆形路径，随后调用clip方法将该圆形路径设置为当前活动裁剪区域——此后所有绘制操作仅在圆形内部区域可见。第三阶段：以计算得出的锚点坐标和缩放尺寸为参数调用drawImage方法，将源图像像素数据按照仿射变换映射渲染至裁剪区域内。第四阶段：调用restore方法从状态栈弹出恢复原始绘图状态，同时将裁剪区域重置为全画布矩形范围。第五阶段：用户确认裁剪后调用canvas.toDataURL("image/png")将画布像素数据无损编码为PNG格式，以Base64编码的Data URL字符串通过HTTP POST请求提交至服务端，服务端接收后解码为二进制字节流并持久化存储为头像文件。')

body('拖拽交互的坐标追踪采用跨输入设备的统一事件处理机制。在Canvas元素上注册mousedown和touchstart事件作为拖拽起始触发器，事件处理中调用preventDefault阻止浏览器默认行为并将拖拽状态标志置为真，记录当前指针屏幕坐标。在window对象上注册mousemove和touchmove事件作为拖拽进行中的连续追踪器，处理函数检查拖拽标志后计算当前帧与上一帧指针坐标的像素差值，将差值累加至dx和dy偏移量并立即重绘画布以提供实时视觉反馈。在window对象上注册mouseup和touchend事件作为拖拽终止器，将拖拽状态标志重置为假。')

img('（这里需要插入一张头像裁剪坐标系变换示意图）')

h3('4. 基于缓存优先网络回退策略的离线访问算法')
body('为使系统在移动端具备PWA安装和离线能力，系统通过Service Worker接口实现了静态资源缓存与请求拦截。Service Worker是浏览器提供的独立后台线程，可拦截其作用域内所有网络请求。系统采用缓存优先策略：页面发起GET请求时，Service Worker先在Cache Storage中查找匹配的已缓存响应——命中则直接返回不经过网络；未命中则转发至网络，获取成功响应后将其副本存入缓存供后续离线使用。对POST等修改类请求和/files/、/uploadMusic等动态接口路径，Service Worker直接放行不做缓存拦截。')

body('Service Worker生命周期通过三个事件管理：install事件预缓存样式表、脚本、图标和清单文件等核心静态资源；activate事件遍历所有缓存空间，删除与当前版本号不匹配的旧缓存实现版本化清理；fetch事件按HTTP方法和URL路径特征将请求分流至缓存优先或网络直达分支。整套流程实现了从安装期预加载到运行期拦截再到升级时自动清理的完整缓存管理闭环。')

h3('5. 基于两阶段索引映射与递归遍历的评论树渲染算法')
body('服务端以扁平JSON数组返回评论列表，客户端需将其重建为嵌套对话树。算法分为两个阶段。第一阶段遍历列表建立标识符-评论哈希映射以支持O(1)时间查询，同时收集根节点集合（父节点标识符为零）和子节点映射表（以父节点标识符为键的子评论数组）。第二阶段对根节点中的每个顶层评论调用递归渲染函数，传入评论对象和嵌套深度参数，函数根据深度设置视觉缩进、生成包含作者信息和内容的HTML片段及嵌套子容器，随后查询子节点映射表——若非空则对每条子评论以深度加一递归调用自身，若为空则递归终止。算法整体时间复杂度为O(n)，全过程在客户端完成不产生额外网络往返，评论树从数据到视图的转换在页面加载后瞬间完成。')

# ==================== 结论与展望 ====================
pb()
h1('结论与展望')

h2('一、工作总结')
body('本文以算法设计与模型构建为贯穿全文的叙述主线，系统化地阐述了基于浏览器-服务器架构的音乐社交系统——"ZY音乐"的完整技术实现路径与核心算法设计。')

body('服务端算法层面，系统基于分层架构模式构建了页面表示、请求分发、业务规则、持久访问和物理存储的五层数据处理模型，实现了各逻辑层次之间的关注点有效分离。评论数据组织上，采用邻接表模型以单一关系表统一承载音乐评论和帖子评论两种场景，通过父节点自引用字段编码任意深度的嵌套层级关系，并配套完成了服务端单次扁平查询与客户端两阶段递归树形重建的完整数据处理算法链。音乐检索上，基于SQL LIKE通配符模式匹配实现了覆盖歌曲名称、艺术家、流派和上传者四个维度的模糊搜索功能。交互状态管理上，利用检查-切换原子操作模式保证点赞标记翻转的逻辑一致性，借助WAL预写式日志的单连接串行化语义在不引入分布式协调开销的前提下实现并发安全。流媒体传输上，完整实现了包含请求解析、边界校验、文件指针精确定位和分段缓冲写入四个环节的HTTP Range字节级流式传输算法。数据库迭代管理上，设计了基于DatabaseMetaData模式元数据检测的幂等增量迁移算法，通过幂等建表语法与列级按需补充机制的协同工作，在十五个业务字段的持续追加过程中实现了零数据损失的结构版本化演进。')

body('客户端算法层面，系统基于IIFE单例模式与localStorage持久化存储构建了跨页面全局共享的播放器状态管理模型，以"变更即保存、加载即恢复、卸载前最终持久"的三段式策略解决了传统多页面架构中播放中断和进度丢失的核心体验难题。播放模式控制基于有限状态机模型进行了形式化建模，其中随机模式采用期望采样次数为n/(n-1)的拒绝采样随机化算法以杜绝连续重复播放。头像编辑方面，基于Canvas圆形裁剪路径与二维仿射变换的数学理论，构建了从源图像坐标系到裁剪画布坐标系的完整像素映射管线，全部图像处理在客户端本地完成。离线访问方面，基于Service Worker缓存优先网络回退策略实现了静态资源本地缓存与智能请求拦截。评论渲染方面，利用两阶段索引映射与递归树遍历算法在O(n)时间复杂度内高效完成了扁平JSON列表到嵌套对话树的转换。')

body('系统以嵌入式Jetty Web容器和SQLite本地关系型数据库为运行时基础设施，以JavaFX WebView桌面端封装和PWA渐进式Web应用配置为多终端交付手段，实现了浏览器、桌面客户端和移动端三种形态的统一访问与一致体验。在整个项目的算法选型与工程实现过程中，始终遵循理论可行性与工程实用性相平衡的技术决策原则，所采纳的数据结构与算法方案均为经充分验证的成熟技术。')

h2('二、未来展望')
body('安全性上，当前身份凭据以原文存储，后续应引入基于自适应哈希的不可逆加密存储方案。Provos和Mazières提出的Bcrypt算法通过内建随机盐值和可配置迭代轮数，能有效抵御彩虹表攻击和暴力破解，并可配合登录频次限制和跨站防护令牌构建多层安全防御体系。音乐检索上，当前基于通配符匹配的搜索缺乏结果相关性排序，未来可引入基于TF-IDF向量空间模型或BM25概率排序函数的全文检索算法，并探索基于协同过滤或声学特征表示的内容推荐模型，从被动搜索向主动发现演进。部署架构上，未来可通过抽象可插拔的存储后端接口支持远程关系数据库和对象存储服务的透明切换，使系统具备从单机部署向多用户服务器部署迁移的能力。')

# ==================== 参考文献 ====================
pb()
h1('参考文献')

refs = [
    '[1] Gamma E, Helm R, Johnson R, Vlissides J. Design Patterns: Elements of Reusable Object-Oriented Software[M]. Boston: Addison-Wesley, 1995.',
    '[2] Fowler M. Patterns of Enterprise Application Architecture[M]. Boston: Addison-Wesley, 2002.',
    '[3] Alur D, Crupi J, Malks D. Core J2EE Patterns: Best Practices and Design Strategies[M]. 2nd ed. Upper Saddle River: Prentice Hall, 2003.',
    '[4] Codd E F. A Relational Model of Data for Large Shared Data Banks[J]. Communications of the ACM, 1970, 13(6): 377-387.',
    '[5] Fielding R, Reschke J. RFC 7233: Hypertext Transfer Protocol (HTTP/1.1): Range Requests[S]. IETF, 2014.',
    '[6] Fielding R T. Architectural Styles and the Design of Network-based Software Architectures[D]. Irvine: University of California, Irvine, 2000.',
    '[7] Celko J. Trees and Hierarchies in SQL for Smarties[M]. 2nd ed. San Francisco: Morgan Kaufmann, 2012.',
    '[8] Krasner G E, Pope S T. A Cookbook for Using the Model-View-Controller User Interface Paradigm in Smalltalk-80[J]. Journal of Object-Oriented Programming, 1988, 1(3): 26-49.',
    '[9] Provos N, Mazières D. A Future-Adaptable Password Scheme[C]//Proceedings of the 1999 USENIX Annual Technical Conference. Monterey: USENIX Association, 1999: 81-91.',
    '[10] Garrett J J. Ajax: A New Approach to Web Applications[EB/OL]. (2005-02-18). https://adaptivepath.org/ideas/ajax-new-approach-web-applications/.',
    '[11] Pilgrim M. HTML5: Up and Running[M]. Sebastopol: O\'Reilly Media, 2010.',
    '[12] Flanagan D. JavaScript: The Definitive Guide[M]. 7th ed. Sebastopol: O\'Reilly Media, 2020.',
    '[13] Hunter J, Crawford W. Java Servlet Programming[M]. 2nd ed. Sebastopol: O\'Reilly Media, 2001.',
    '[14] Hickson I, Berjon R, Faulkner S, et al. HTML5: A Vocabulary and Associated APIs for HTML and XHTML[S]. W3C Recommendation, 2014.',
    '[15] Kaminskas M, Ricci F. Contextual Music Information Retrieval and Recommendation: State of the Art and Challenges[J]. Computer Science Review, 2012, 6(2-3): 89-119.',
    '[16] Schedl M, Zamani H, Chen C W, et al. Current Challenges and Visions in Music Recommender Systems Research[J]. International Journal of Multimedia Information Retrieval, 2018, 7(2): 95-116.',
    '[17] Freeman E, Robson E, Bates B, et al. Head First Design Patterns[M]. 2nd ed. Sebastopol: O\'Reilly Media, 2020.',
    '[18] Crockford D. JavaScript: The Good Parts[M]. Sebastopol: O\'Reilly Media, 2008.',
]

for ref in refs:
    ref_line(ref)

# ==================== Save ====================
output_path = 'F:/ZYmusic(2)/zy音乐（1）.docx'
doc.save(output_path)
print(f'Saved: {output_path}')
total = sum(len(p.text) for p in doc.paragraphs)
print(f'Paragraphs: {len(doc.paragraphs)}')
print(f'Characters: {total}')
